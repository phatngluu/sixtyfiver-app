import { BeautifySizePipe } from './beautify-size.pipe';

describe('BeautifySizePipe', () => {
  it('create an instance', () => {
    const pipe = new BeautifySizePipe();
    expect(pipe).toBeTruthy();
  });
});
